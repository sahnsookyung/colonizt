name: CD - Build & Push Images

on:
  push:
    branches:
      - main
  workflow_dispatch:

permissions: {}

concurrency:
  group: cd-${{ github.ref_name }}
  cancel-in-progress: false

jobs:
  build-and-push:
    name: Build & Push (${{ matrix.service }})
    runs-on: ubuntu-latest
    timeout-minutes: 45
    permissions:
      contents: read
      packages: write
    strategy:
      fail-fast: false
      matrix:
        include:
          - service: server
            dockerfile: ./Dockerfile.server
            image: colonizt-server
          - service: web
            dockerfile: ./Dockerfile.web
            image: colonizt-web
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          persist-credentials: false

      - name: Set up QEMU
        uses: docker/setup-qemu-action@v3

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ github.token }}

      - name: Build and push ${{ matrix.service }}
        uses: docker/build-push-action@v6
        with:
          context: .
          file: ${{ matrix.dockerfile }}
          platforms: linux/arm64
          push: true
          tags: |
            ghcr.io/sahnsookyung/${{ matrix.image }}:latest
            ghcr.io/sahnsookyung/${{ matrix.image }}:${{ github.sha }}
          build-args: |
            VITE_API_BASE_URL=https://colonizt.sookyungahn.com
          cache-from: type=gha,scope=colonizt-${{ matrix.service }}
          cache-to: type=gha,mode=max,scope=colonizt-${{ matrix.service }}
